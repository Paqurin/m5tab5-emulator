#include <gtest/gtest.h>
#include "emulator/connectivity/bluetooth_controller.hpp"

namespace emulator::connectivity::test {

TEST(BluetoothControllerTest, BasicTest) {
    EXPECT_EQ(1 + 1, 2);
}

} // namespace emulator::connectivity::test