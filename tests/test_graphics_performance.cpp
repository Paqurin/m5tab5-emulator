#include <gtest/gtest.h>

namespace emulator::graphics::performance::test {

TEST(GraphicsPerformanceTest, BasicTest) {
    EXPECT_EQ(1 + 1, 2);
}

} // namespace emulator::graphics::performance::test