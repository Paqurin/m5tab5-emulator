// Temporary stub implementations for GUI classes to allow compilation
#include "emulator/gui/main_window.hpp"
#include "emulator/utils/logging.hpp"

namespace m5tab5::emulator::gui {

// Stub implementations for incomplete GUI classes

// DockManager stub
class DockManager {
public:
    DockManager() = default;
    ~DockManager() = default;
};

// HardwareMonitor stub  
class HardwareMonitor {
public:
    HardwareMonitor() = default;
    ~HardwareMonitor() = default;
};

// LogViewer stub
class LogViewer {
public:
    LogViewer() = default;
    ~LogViewer() = default;
};

// MemoryInspector stub
class MemoryInspector {
public:
    MemoryInspector() = default;
    ~MemoryInspector() = default;
};

} // namespace m5tab5::emulator::gui